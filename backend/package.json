{
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3"
  },
  "scripts": {
    "start": "node build/index.js",
    "build": "npx tsc",
    "build:start": "npm run build && npm start"
  }
}
