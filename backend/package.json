{
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "@sentry/node": "^9.1.0",
    "@sentry/profiling-node": "^9.1.0",
    "dotenv": "^16.4.7",
    "fastify": "^5.2.1",
    "posthog-node": "^4.6.0"
  },
  "scripts": {
    "start": "node build/index.js",
    "build": "npx tsc",
    "build:start": "npm run build && npm start"
  }
}
