{
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "fastify": "^5.2.1"
  },
  "scripts": {
    "start": "node build/index.js",
    "build": "npx tsc",
    "build:start": "npm run build && npm start"
  }
}
